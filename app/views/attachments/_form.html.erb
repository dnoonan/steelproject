<%= simple_form_for(@attachment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.association :user %>
    <%= f.association :artifact %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
